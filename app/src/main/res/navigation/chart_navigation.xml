<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/chart_navigation"
    app:startDestination="@id/chart_fragment">

    <fragment
        android:id="@+id/chart_fragment"
        android:name="com.gholem.moneylab.features.chart.ChartFragment"
        android:label="Chart"
        tools:layout="@layout/fragment_chart">
        <action
            android:id="@+id/action_chart_fragment_to_history_navigation"
            app:destination="@id/history_navigation" />
    </fragment>
    <include app:graph="@navigation/history_navigation" />
</navigation>
